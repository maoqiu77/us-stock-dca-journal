# Phase 6 / 7 交接（2026-09-21）

结论：本地实现与项目自动化已完成本轮修复，开发者工具部分实测通过；真机和服务发布条件未完成，不具备上线结论。没有生产发布、云函数部署、Git commit/push、模型生成或图片上传。用户明确选择自行预览、进行真机验收。

## 改动与边界

- 完整恢复原先顺序提交金融账本、手记根指针和自选，中断会混合新旧数据。新增 `restore-transaction.ts`，先在内存形成完整写集合，再逐值暂存和 SHA-256 校验，最后持久化提交决定。决定前故障保留旧完整数据；决定后读写必须先续完已确认的本地替换；没有联网重放。篡改/未知事务版本停止读写。
- 同一机制覆盖 v1–v7 完整恢复、新建空账本和恢复点回退；恢复点恢复对应自选，保留有意清空的列表。仍使用金融 v3、完整备份 v7，没有新金融迁移。
- 恢复预览补齐持仓校准数、导入回执、会话、来源和自选数；确认提示旧备份可能恢复已删除内容。移除设置页重复“关于”卡片；补齐通用窄屏换行和底部安全区。
- 生产页面原已无载入演示入口，本轮将检查扩展到全部页面的绑定事件；fixture/fake provider 仅隔离测试/开发注入。已有 demo 标识记录不删除、不改成真实记录。默认 30 个公开标的目录不是持仓或示例报价。
- 保留开始时全部未提交改动；本轮没有重置 Git、删除用户账本或动旧 Streamlit 项目。实际 UI 测试仅输入未保存的合成表单和非法备份；没有点击恢复、保存或删除真实数据。

## 验证

最终命令与日志见 [evidence/phase67](evidence/phase67)，逐用例见 [ACCEPTANCE.md](ACCEPTANCE.md)。小程序 232/232、网关 90/90、行情 6/6、领域 35/35、AI 上下文 28/28、API 174/174、Web 74/74；相关 TypeScript 检查、生产构建/包边界、public-safety 通过。主包 1,623,251 bytes，14 页。核心 hash 和云包 hash 见 build-identity.json；云包仅本地构建，不能据此推定云端版本一致。

新增集成测试直接调用项目 service/repository，逐写点注入写前/写后错误、重启、损坏暂存、恢复点、自选、重复恢复、截图审核字段→分析→编辑手记→日历→删会话→恢复预览。AI 使用明确隔离的 fake provider；不代表真实识别或模型精度。v4/v5 补齐迁移 roundtrip；v1/v2/v3/v6/v7 沿用实际项目回归。MP0/MP1 手算基线成本 951.00、已实现 89.00 保持一致。

## 开发者工具操作记录

- 工程 `apps/miniprogram/dist`；Stable 2.02.2608070；实际基础库 3.17.2（源码最低配置 3.7.6 未实测）。编译后控制台 0 errors，存在 App.onLaunch 性能、资源 preload、自动热重载警告。
- 320px / iPhone 5 / 字体 26：持仓页标题、按钮和已有长名称可见；添加→手动表单可打开；输入合成长名称、滚动到底部，“检查”按钮完整可达。未保存。见 devtools-320-form.png、devtools-320-bottom.png。
- 375px / iPhone X / 字体 26：持仓页和底部安全区可见；设置页粘贴 version=99 合成备份→“检查备份”明确拒绝，不出现恢复预览。清除输入。见 devtools-375-invalid-backup.png。
- 430px / iPhone 14 Pro Max / 字体 26：持仓首页标题、按钮和长名称没有观察到横向裁切；切到美股看板成功显示报价，切到 ETF 出现加载态。曾在 UI 读取到 ETF 行情日与缺失 60 日分位；没有逐标的对照外部源，不等于价格真实性全验收。
- 工具数次报告用户同时改变界面，重新读取状态后继续；未以失败点击作为成功证据。三尺寸不是所有页面的完整矩阵，U01/U02 仍 PARTIAL。系统键盘、前后台/断网、Android/iOS 尚未验证。
- 截图只保存不含真实持仓内容的合成表单/错误态；真实账本页面截图没有保存到仓库。

## 用户操作与管理员操作

用户：在现有开发者工具对 dist 点“预览”，用 [DEVICE_ACCEPTANCE.md](DEVICE_ACCEPTANCE.md) 的 1–12 项记录设备/微信版本/构建 hash。先在可信位置导出当前完整备份；破坏性/恢复测试只用隔离测试工作区。真机结果目前全部待本轮确认。

管理员：不把页面声明代替后台核验。核验合法域名、隐私保护指引、选图/文件接口、云存储和数据库 ACL、timer 实际清理、服务端身份和权限、公开行情展示/缓存/AI归档许可、平台类目/备案、最低基础库。部署和模型调用需要另行授权，本轮不执行。

当前服务端环境变量名称从现有适配器复核：`EXPECTED_WEAPP_APPID`、`AI_ENABLED`、`DEEPSEEK_API_KEY`、`AI_VISION_ENABLED`、`AI_VISION_MODEL`；行情含 `MARKET_PROVIDER`、`MARKET_ENABLED`、`MARKET_ACCESS_MODE`、`MARKET_CONSENT_VERSION`、`RESEARCH_MARKET_ENABLED`、`CN_MARKET_PROVIDER`、`CN_MARKET_ENABLED`；选择 Twelve Data/Tushare 时分别使用 `TWELVE_DATA_API_KEY` / `TUSHARE_TOKEN` 及现有授权配置。实际完整名称以 cloud/*/index.js 为准，不复制凭证，不在客户端设置 Key。公开接口可达不等于拥有公开再分发许可。

## 回滚

1. 先在本版完成任何待恢复事务，导出并验证完整 v7 备份。若 `portfolio.wechat.restore.v1` 非空/损坏或设备仍空间不足，禁止降级：旧程序不识别新恢复屏障。不得手删 marker 或暂存键。
2. 本轮开始时源文件副本在 `/tmp/chicang-phase67-before-20260921`；本轮逆向差异保存在 evidence/phase67/rollback.patch。使用前 `git apply --check`，确认没有后续修改冲突后才应用，禁止 `git reset --hard`。它不撤销用户此前未提交改动，也不替换用户数据。
3. 恢复源文件后执行 `npm run check:weapp && npm run check:weapp-production`，重新在开发者工具编译；若失败继续留在本版和保留备份，不回退数据库。
4. 已把开始时旧 service 打包并读取本版完成恢复后的合成数据，完整导出 data 相等、成本 21.00、原文相等（rollback.log）。仅证明完成事务后回到本轮开始代码兼容；更老版本、进行中的事务和真机降级未验证。

容量检查保守预留暂存及最终数据双份空间，接近微信配额时宁可拒绝恢复。旧备份可能包含已删内容；恢复是用户显式替换，不是自动合并。此次范围不包含清除所有历史孤立分区的取证级擦除。
