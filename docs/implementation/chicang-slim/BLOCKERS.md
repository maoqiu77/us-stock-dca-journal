# 外部阻塞与未完成项

## 真实视觉服务与识别样本

- 状态：`PARTIAL_REAL_ENV`
- 当前缺少：`portfolioAiCleanup` 真实定时触发器和过期/重试候选删除证据；前导零、多账户、截断、盈亏平衡价专项视觉样本仍未真实回归。
- 已完成：三个私有集合、provider 边界、私有 task/file owner 校验、结构/MIME/尺寸/像素限制、严格输出 schema、超时/幂等/单用户并发上限、即时清理、可编辑审核和原子导入；两张授权样本的真实识别与即时删除已通过。
- 不能声称：真实图片完整解码库验收通过、所有券商/基金截图精度已达标，或定时器已实际删除过期对象。
- 最小下一步：为 `portfolioAiCleanup` 配置并验证定时触发，再补专项视觉样本；不写入真实持仓。
- 2026-09-20 核查：本地网关类型检查及视觉/清理聚焦回归 15/15 通过；现有客户端 CloudBase 绑定仅能确认是既往 A2/A3 环境，不能据此认定为独立非生产环境。未找到获授权的脱敏截图或可用部署通道，未进行云端写入、模型调用和对象删除。继续前需明确测试环境 ID/非生产属性及访问方式，并提供已授权、无姓名/账号等隐私的截图样本与逐行预期值；确认该环境的私有存储规则、视觉服务配置及定时触发器权限后，才可记录真实识别差错和删除证据。
- 后续授权与实际进展：用户明确允许使用现有 `cloud1` 和少量付费调用；已部署最新视觉/清理代码、启用视觉变量，并创建 `vision_tasks`（仅创建者可读写）。本地私有目录已生成虚构样本与预期值，未使用真实账户数据。当前小程序能力检查仍显示不可用；须先补齐/核验 `vision_upload_keys`、`vision_usage`，取得 `visionCapabilities` 成功回执，再上传样本。尚未发生图片外发、视觉模型调用或云对象删除。
- 真实调用定位：用户新授权的第一张截图在微信开发者工具中触发识别后，云端 `createHoldingUpload` 因 `vision_upload_keys` 集合不存在失败（`DATABASE_COLLECTION_NOT_EXIST`），发生于上传文件之前；第二张未试。CloudBase 存储权限为“仅创建者可读写”；`portfolioAiCleanup` 的定时触发器为空。须补建两个集合并核对数据权限、重试两张图、记录逐行差错及实际删除回执，随后配置并验证定时清理。此前“能力检查仍显示不可用”是早期页面状态，不应替代这次真实云端错误。
- 已关闭的部分：`vision_upload_keys`、`vision_usage` 已按仅创建者可读写创建；CloudBase `_id` 回写和嵌套 response 持久化兼容问题已修复并部署。两张授权图均生成审核草稿且没有导入持仓；股票 5 行代码/数量匹配、成本留空，基金 2 行名称匹配、数量留空。成功任务 `objectCleaned=true`、`cleanupPending=false`，对象存储为空。
- 剩余阻塞：`portfolioAiCleanup` 的定时触发器仍为空，尚未验证过期/重试候选在真实云环境由定时任务删除；前导零、多账户、截断、盈亏平衡价等专项视觉样本也未做真实模型回归。P2-06 的当前两张样本和即时删除已通过，但不能把定时兜底与全部识别矩阵标为 PASS。
- 每日识别次数已解除：测试环境 `portfolioAi` 使用 `dailyLimit: null`，不受旧环境变量覆盖；保留每用户 1 个处理中任务。同日超原 3 次后的真实识别已成功，该项不再是阻塞。

## 微信开发者工具 / 真机视觉验收

- 状态：`FAILED_DEVTOOLS_AND_PENDING_DEVICE`
- 当前结果：开发者工具已实际运行。320px 手动添加路由出现横向双页/窄切片，375px 也观察到页面切换后底部表单切片，U01 不能通过；430px 首页与最大字体可读，普通 390px 表单主按钮可滚动到且未被安全区遮挡。
- Android 真机证据：用户扫码预览并提供首页截图；其余首页内容和底部安全区主观正常，但右上角绿色“＋”按钮整体向屏幕外溢出，仅剩左部可见。全局 `.actions button { flex: 1 }` 与页头 `.actions` 共用可能是直接布局原因，修复前不能将 Android 首页或 U01 标为通过。
- Android 真机键盘已关闭：用户确认手动添加的数量输入、键盘弹出、滚动及底部主按钮均正常。剩余真机范围为截图审核、前后台恢复及修复页头横向溢出后的回归。
- 标的详情页右上“校准”也由用户真机截图证实裁切；两个页头已在本地改为标题下方独立按钮行，开发者工具 320/375/430px 与 26 号字体复核可见。当前阻塞改为新预览包的 Android 真机回归，而非代码尚未修复；旧预览包仍会显示原问题。
- 真机缺少：用户扫码打开已生成的预览包，并在实际 iOS/Android 上完成键盘、前后台、底部安全区与长文字检查。
- 已完成：14 页面 test/production 构建、WXML handler 检查、页面控制器与 packaged runtime 回归。
- 不能声称：320/375/430、大字体、长名称、键盘、安全区、滚动定位和 sticky 按钮在真机通过。
- 最小下一步：先定位并修复 320/375px 页面横向转场/容器溢出，再重新跑三尺寸；同时由用户扫码预览包完成真机键盘和安全区检查。

## 非 US/USD 真实交易与行情

- 状态：`BLOCKED_EXTERNAL`，属于 Phase 4 范围。
- 当前边界：CNY/USD checkpoint 按币种隔离；非 US/USD 可保存 `unverified` 当前持仓；真实交易 domain 与现有行情仍受原许可/目录约束。
- Phase 4 已有三分段页面与缺失/缓存状态；尚未新增国内真实行情、净值供应商或非 US 真实交易能力。

## 明确停止边界

- Phase 4/5 已执行本地代码与测试；Phase 6/7：`NOT_STARTED`。
- 生产部署、Git commit/push：`NOT_RUN`。非生产 `cloud1` 部署和必要的少量付费视觉调用已在用户授权范围内执行。


## Phase 4/5 本轮剩余外部验收

- `BLOCKED_EXTERNAL`：国内场内 ETF / 场外 QDII 的获授权目录、价格、正式 NAV/IOPV 与披露时间源未配置；不能展示核实的默认产品、真实净值或折溢价。已完成通用事实/折溢价校验和 UI 空态；最小下一步是确定供应商及展示/缓存/AI 归档许可后接入现有服务端边界。
- 当前开发者工具看板返回行情暂不可用。US Twelve Data 既有适配和服务端缓存继续复用；本轮未订购、部署或使用付费模型。真实报价和 2–3 候选的云端回答尚未完成端到端验收。
- iPhone X / 26 号字体本地空态和 AI 入口已检查，完整 320/375/430、长名称、大字体、真机往返需要进一步验证；未声称全部通过。
- 主包仍接近 2 MiB 上限，构建已移除无使用的图表组件打包并压缩页面 JS，继续保留硬性体积检查。
- 净值/IOPV 通用契约以 `@portfolio/market-data/reference` 独立入口提供，避免未接通的国内参考值代码进入当前美股客户端包；需在真实国内 adapter 接入后连到页面数据。


## 最新覆盖说明：公开版补齐后（2026-09-20）

上面“尚无国内 adapter/尚未连页面”记录已由本轮代码关闭：Tushare adapter、domesticBoard action、两个国内分段及持仓表单已接通并以合成测试验证。

仍缺：Tushare 服务端 token/API 权限、公开展示/缓存许可及有效期、核实的 CNY 产品代码白名单、新 portfolioMarket 测试部署；Twelve Data 真实权限/配置和行情成功回执；实时 IOPV；国内行情 AI 归档许可及对应凭据代码；真实 AI 比较端到端；清理定时器及专项截图；完整真机矩阵。不能说只填 token 就全部完成。

当前私有客户端 marketFunctionName 已补齐。供应商配置是否存在于云端尚未核实；本机环境未发现可用 TWELVE_DATA_API_KEY/TUSHARE_TOKEN，不读取/打印云端秘密。


## Phase 6/7 当前覆盖（2026-09-21）

本条覆盖上方“Phase 6/7 NOT_STARTED”。P6-01/02/03/05 本轮项目回归通过；P6-04 本地隔离与服务端合成权限回归通过，真实双账号/云 ACL 仍未验收。P7-01/03/05/06 本地实现和交接完成；P7-02 开发者工具部分完成，真机按用户回复由用户自行预览验收；P7-04 外部发布条件未完成。详细证据、逐项 H/V/J/M/D/U 状态、回滚见 [PHASE67_HANDOFF.md](PHASE67_HANDOFF.md) 与 [ACCEPTANCE.md](ACCEPTANCE.md)。

继续阻塞：Android/iOS 完整矩阵、系统键盘与前后台/断网、真机文件分享恢复；真实双账号权限、完整图片解码及专项视觉样本、云端 cron 删除；行情同日源逐项对照、展示/缓存/AI 归档许可；小程序后台域名/隐私/类目/备案、最低基础库。不得把可达公开接口或历史成功样本当成本轮全验收。进行中的恢复事务不可降级；更旧软件兼容未验证。没有可上线结论。

本轮未生产发布、未部署云函数、未调用模型、未上传图片、未提交或推送 Git。按用户要求交付后停止。
