# 行情云函数部署记录（2026-09-20）

用户在本任务明确回复“允许”，授权将 portfolioMarket 上传到既有 cloud1。

## 已执行及证据

- 仅上传 `apps/miniprogram/dist/cloudfunctions/portfolioMarket`，采用微信开发者工具“云端安装依赖”。未上传 portfolioAi / portfolioAiCleanup，未发布小程序或提交审核。
- 云控制台确认 portfolioMarket 已部署，初次创建/部署时间为 2026-09-20 19:28:53。该函数之前尚不存在。
- 初次调用出现 `-504002 / 145 code exit unexpected`。配置界面证实默认运行环境 Node.js 16.13，超时 3 秒，环境变量为空。
- 增加 Node 16 的 undici 5.29.0 fetch 兼容与 AbortSignal.timeout 兼容；重新上传函数并云端安装依赖。
- 设置 EXPECTED_WEAPP_APPID 为当前工程 AppID，超时调整为 30 秒，内存保持 256 MB。未改其他函数配置。
- 从实际小程序逻辑层调用 capabilities，得到 `ok=true, enabled=true, provider_configured=true, authorized=false, access_mode=closed_beta`；provider 为东方财富公开接口，报价/搜索为 true，AI 来源/归档/历史 K 线为 false。
- 服务端检查 72/72 通过，包含 Node 16 bootstrap 回归；公开安全检查通过。

最终上传本地文件树摘要（非云端下载比对）：

`06f5e88b44876352375873d3cb8cac3d73c5caef318d56e7a275443097046a3f`，4 文件，437308 字节。

## 尚未通过

用户随后明确回复“允许，后续都允许”。已为当前测试账号写入 market-v1 许可（enabled/enrolled），并创建 market_access、market_cache、market_leases、market_usage；四个集合均设置为客户端所有用户不可读写，云函数管理访问。没有开放其他账号或改动 AI 授权。

开发者工具实测 capabilities 返回 authorized=true。美股页面成功显示 NVDA 222.27 / +2.930、AAPL 336.13 / -0.870 及对应头像（本次观测值，非保证实时）。ETF 页面显示行情日 2026-09-18；159501 价格 2.129、参考溢价 14.25%、份额 678698.65 万份、增量 100 万份，并显示三项基准指数。列表与卡片切换已验证。

修复国内列表嵌套 wx:key 警告，改为顶层 instrument_key。207 项小程序测试、生产包检查、公开安全检查通过；主包 1540.8 KiB。账号配置期间首次编辑器提交产生一条无许可字段的空记录，不能通过访问检查；保留，未删除任何记录。

历史 60 日溢价回填仍缺，当前留空；云端部署成功不代表此字段已完成。尚未执行真机验收。

场外基金成功路径也已在开发者工具验证：017730 单位净值 4.0806 CNY，净值日 2026-09-17，未知公告日显示 --。

## 日 K 详情页后续部署（2026-09-20）

用户明确授权完成后续构建、部署和验证。更新既有 portfolioMarket，仍只部署此函数、云端安装依赖。新增美股历史日线与总市值；东方财富历史接口断连时尝试腾讯财经前复权日线，最后备用 Yahoo 拆股调整日线，各来源明确标注口径。仅纳入纽约日期早于当前日期的日线。

云端只读 bars 请求实测成功返回 320 条 NVDA 日线；开发者工具页面显示 MA5 215.728、MA20 218.721、MA60 211.274，来源腾讯财经、日线截至 2026-09-18。初始调用曾遇到供应商限频与网络失败，公开接口可用性不能视为保证。

已更新图表为 Canvas 2D。重新编译后未再复现 getActiveAppWindow，项目源码及产物无该调用；开发工具预加载、热重载、worker reportRealtimeAction 提示仍存在。未发布小程序或提交审核，未真机验收。

本次行情云函数本地上传树摘要：1483c32f40199ecaf00334bc91f4ff3aee5fb77e91aa6c6dade0cd92ff891f11，441353 字节，4 文件（并非下载云端比对）。小程序 214 项回归通过。

## AI 研究多市场、多周期与界面调整

用户确认完整扩展 A 股、港股、美股及日线、60/30/15/5/1 分钟。新建 market_receipts_private，权限为客户端所有用户不可读写；portfolioMarket 与 portfolioAi 已上传并部署，AI 使用服务端冻结行情，不信任客户端价格。未进行真实模型生成或上传真实持仓作测试。

看板新标的插入首位。投资计划改为最多 2000 字的自由文本；持仓分析默认勾选我的持仓与投资计划，取消勾选后不包含对应来源。研究表单为市场、代码、四张 2×2 快选卡片、左右主/辅助周期、可选数量/成本/最大持仓。修复首次云处理确认前分析按钮被隐藏的问题。

参考股票交易平台-next 的 Yahoo chart 接口；未迁移其示例行情回退及价格采样点当 K 线的逻辑。美股分钟增加新浪真实 OHLC 备用源，按每根 K 线的纽约时区处理夏令时；港股增加东方财富真实一分钟 OHLC 聚合，按上午/下午交易段及收盘边界聚合，明确来源、单位和时间标签。

云端只读验收：NVDA 日线及五种分钟周期均返回 120 根；600519 日线及五种分钟周期均返回 120 根。DevTools 已检查 2×2 卡片和左右周期布局。未调用真实模型：使用注入测试验证可信 K 线到达模型适配器、异标的凭证被拒绝及 ACK 清理。

本地验证：小程序 217/217、网关 82/82、市场契约 6/6；类型检查与生产包检查通过。小程序主包约 1565.8 KiB（低于 2 MiB 硬限制，仍高于 DevTools 1.5 MiB 建议值）。未发布小程序、未真机验收、未推送 Git。公开数据源延迟与可用性无保证，数据不足或失败明确显示，不补造数据。

港股后续云端验收：00700 日线 120 根、60 分钟 6 根、30 分钟 11 根、15 分钟 22 根、5 分钟 66 根、1 分钟 120 根，均截至 2026-09-18。分钟备用接口本次只提供最近一交易日，较长周期样本不足必须保持显式数量与来源，不能声称有 120 根历史或足够长期判断。最新 portfolioMarket 上传后页面已实际返回东方财富聚合来源，证明新增云端路径生效。

## AI 首次确认与执行故障修复

首次确认弹窗按钮超过微信四字限制，已改为“同意继续”，弹窗失败不再误报用户取消。云端已有 ai_access 文档包含只读 _id；保存 consent 改为更新同意字段，保留原访问许可。数据库写入失败与真正 ACCESS_DENIED 使用不同提示。补齐 ai_global_usage，客户端所有用户不可读写。

行情凭证读取剥离数据库 _id，避免绑定时写回只读字段；为两个 SDK 行为添加回归。portfolioAi 增加 Node 16 fetch 兼容并部署。网关 85 项回归通过，生产包检查通过。用户要求调试成功后使用公开 AAPL 日线进行真实链路验证，无持仓数量/成本、无投资计划、无个人手记；首个测试在凭证绑定阶段失败，原请求未自动重试。

后续真实调用返回 PROVIDER_OUTPUT_TRUNCATED。云端检查确认 AI_MAX_OUTPUT_TOKENS 实际为 800，已改为 3000；Node.js 实际运行时为 20.19，45 秒执行超时保持不变。新增中文短报告长度指令，保留 JSON/source_ids 严格校验，部署 portfolioAi。历史失败请求未自动重复发送。

最终真实验收成功：AAPL 公开日线分析后自动进入 pages/conversation/index，会话显示“模型生成”、中文趋势分析及来源查看按钮，AI 服务可用。测试未携带实际持仓、计划或个人手记。本地会话保存已通过页面验证；未完成云端 ACK 文档的独立复查，不声称已独立验证云端正文清理。
